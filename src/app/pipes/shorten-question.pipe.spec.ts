import { ShortenQuestionPipe } from './shorten-question.pipe';

describe('ShortenQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
