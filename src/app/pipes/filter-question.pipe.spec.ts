import { FilterQuestionPipe } from './filter-question.pipe';

describe('FilterQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
